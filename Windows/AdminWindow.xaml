<Window x:Class="BurgerPlusManagementSystem.Windows.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BurgerPlusManagementSystem.Windows"
        mc:Ignorable="d"
        Title="BurgerPlus - Администратор"
        Height="650"
        Width="1100"
        MinHeight="650"
        MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление пользователями"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,15">

            <Button x:Name="btnAdd"
                    Width="150"
                    Height="40"
                    Margin="0,0,10,0"
                    Content="Добавить"
                    Background="#FF33E602"
                    Click="btnAdd_Click"/>

            <Button x:Name="btnEdit"
                    Width="150"
                    Height="40"
                    Margin="0,0,10,0"
                    Content="Изменить"
                    Click="btnEdit_Click" Background="#FF1B8CF5"/>

            <Button x:Name="btnUnblock"
                    Width="180"
                    Height="40"
                    Margin="0,0,10,0"
                    Content="Разблокировать"
                    Click="btnUnblock_Click" Background="#FFDC3E54"/>

            <Button x:Name="btnRefresh"
                    Width="150"
                    Height="40"
                    Content="Обновить"
                    Click="btnRefresh_Click" Background="#FFE6CD25"/>

        </StackPanel>

        <DataGrid x:Name="dgUsers"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Background="Beige"
                  FontSize="16">

            <DataGrid.Columns>

                <DataGridTextColumn Header="ID"
                                    Binding="{Binding UserID}"
                                    Width="70"/>

                <DataGridTextColumn Header="Фамилия"
                                    Binding="{Binding UserSecondName}"
                                    Width="*"/>

                <DataGridTextColumn Header="Имя"
                                    Binding="{Binding UserFirstName}"
                                    Width="*"/>

                <DataGridTextColumn Header="Отчество"
                                    Binding="{Binding UserPatronymic}"
                                    Width="*"/>

                <DataGridTextColumn Header="Логин"
                                    Binding="{Binding Login}"
                                    Width="*"/>

                <DataGridTextColumn Header="Роль"
                                    Binding="{Binding Roles.RoleName}"
                                    Width="150"/>

                <DataGridCheckBoxColumn Header="Заблокирован"
                                        Binding="{Binding IsBlocked}"
                                        Width="120"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</Window>
